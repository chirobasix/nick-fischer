---
import PageLayout from '../layouts/PageLayout.astro';
import { Icon } from 'astro-icon/components';
import {
  abs, withBrand, PERSON_ID,
  buildPerson, buildOrganization, buildWebSite, buildWebPage,
  buildSpeakerEvents, buildFaqPage, type SpeakingEvent,
} from '../lib/seo';

const title = withBrand('Speaking — Keynotes for Chiropractic Events');
const description =
  'Book Nick Fischer to speak. Keynotes and workshops on chiropractic practice growth, marketing systems, patient generation, and AI for chiropractors — for events nationwide.';

// Real engagements only — never fabricate.
const SERIES = "Winner's Edge – True Chiro Success Seminar";
const MOA = 'JW Marriott, Mall of America';
const events: SpeakingEvent[] = [
  {
    name: "Winning Google's Agentic Search",
    event: SERIES, startDate: '2026-06-06', location: MOA,
    videoUrl: 'https://www.youtube.com/watch?v=BXibFkB2tXI',
  },
  {
    name: 'AI for Chiropractors in 2026: The Complete Guide + Live Demos',
    event: SERIES, startDate: '2026-01-17', location: MOA,
    videoUrl: 'https://www.youtube.com/watch?v=6ahP2srFosU',
  },
  {
    name: '4 Pillars of Digital Marketing for Chiropractors',
    event: 'Five Star Management Seminar', startDate: '2025-11-15', location: 'Hyatt Regency – Orlando, FL',
    videoUrl: 'https://www.youtube.com/watch?v=VmSEsOBoHSo',
  },
  { name: 'Rank Higher on Google Maps', event: SERIES, startDate: '2025-01-19', location: MOA },
  { name: 'Get More From Social Media', event: SERIES, startDate: '2024-08-10', location: MOA },
];
// Always render newest-first regardless of array order.
const sortedEvents = [...events].sort((a, b) => (b.startDate ?? '').localeCompare(a.startDate ?? ''));

// Format an ISO date (YYYY-MM-DD) as "January 17, 2026" at build time.
const fmtDate = (iso: string) =>
  new Date(`${iso}T00:00:00`).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });

const topics = [
  { icon: 'lucide:layout-grid', title: 'Marketing as a System', body: 'Why campaigns fail and systems compound — the framework that turns a practice into a patient magnet.' },
  { icon: 'lucide:sparkles', title: 'AI for Chiropractors', body: 'Practical ways to use AI and answer-engine optimization to get found and book more new patients.' },
  { icon: 'lucide:target', title: 'Patient Generation That Lasts', body: 'Paid, local SEO, reputation, and automation working together instead of competing for budget.' },
  { icon: 'lucide:line-chart', title: 'The 5-Year Practice', body: 'Building marketing that grows enterprise value, not just this month\'s lead count.' },
];

const faqs = [
  { question: 'What does Nick Fischer speak about?', answer: 'Chiropractic practice growth, marketing systems, patient generation, local SEO, reputation, CRM automation, and using AI / answer-engine optimization to get found by more patients.' },
  { question: 'What kinds of events does Nick speak at?', answer: 'Chiropractic industry conferences, association meetings, mastermind groups, and corporate or franchise events — keynotes, breakout sessions, and hands-on workshops.' },
  { question: 'How do I book Nick to speak?', answer: 'Use the contact page to share your event date, audience, and format. You\'ll get a fast reply with availability and fit.' },
];

// Featured talk recording (YouTube). Facade-embedded below; VideoObject here.
const TALK_ID = '6ahP2srFosU';

const jsonLd = [
  buildPerson(),
  buildOrganization(),
  buildWebSite(),
  buildWebPage({ url: abs('/speaking/'), name: title, description }),
  buildFaqPage(faqs),
  buildSpeakerEvents(sortedEvents),
  {
    '@type': 'VideoObject',
    name: 'AI for Chiropractors in 2026: The Complete Guide + Live Demos',
    description:
      "Nick Fischer's talk on using AI and answer-engine optimization to grow a chiropractic practice — frameworks plus live demos.",
    thumbnailUrl: `https://i.ytimg.com/vi/${TALK_ID}/maxresdefault.jpg`,
    uploadDate: '2026-01-17',
    embedUrl: `https://www.youtube-nocookie.com/embed/${TALK_ID}`,
    contentUrl: `https://www.youtube.com/watch?v=${TALK_ID}`,
    author: { '@id': PERSON_ID },
  },
].filter(Boolean) as object[];
---
<PageLayout title={title} description={description} jsonLd={jsonLd} preloadImage="/img/speaking-keynote.jpg">
  <section class="page-hero">
    <div class="container-page speak-hero-grid">
      <div class="speak-hero-copy">
        <p class="eyebrow">Speaking</p>
        <h1 class="h-display page-title">Talks that send chiropractors home with a plan.</h1>
        <p class="lead page-dek">
          I speak at chiropractic events across the country on practice growth, marketing
          systems, and AI — energetic, specific, and built so the audience leaves with
          steps they can run on Monday morning.
        </p>
        <a href="/contact/" class="btn btn-primary">Book Nick to speak <Icon name="lucide:arrow-right" width="18" height="18" /></a>
      </div>
      <div class="speak-hero-media">
        <img src="/img/speaking-keynote.jpg" width="1400" height="788" alt="Nick Fischer speaking on stage at a chiropractic event" fetchpriority="high" decoding="async" />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container-page">
      <div class="section-head" data-reveal>
        <p class="eyebrow">Signature topics</p>
        <h2 class="h-display">What I bring to the stage</h2>
      </div>
      <div class="topics">
        {topics.map((t) => (
          <article class="card topic" data-reveal>
            <span class="topic-icon"><Icon name={t.icon} width="22" height="22" /></span>
            <h3>{t.title}</h3>
            <p>{t.body}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="section section-alt">
    <div class="container-page">
      <div class="section-head" data-reveal>
        <p class="eyebrow">Watch</p>
        <h2 class="h-display">A recent talk</h2>
      </div>
      <div class="video" data-yt={TALK_ID} data-reveal>
        <button class="video-btn" type="button" aria-label="Play talk: AI for Chiropractors in 2026">
          <img src={`https://i.ytimg.com/vi/${TALK_ID}/maxresdefault.jpg`} width="1280" height="720" alt="Nick Fischer presenting AI for Chiropractors in 2026" loading="lazy" decoding="async" />
          <span class="video-play" aria-hidden="true"><Icon name="lucide:play" width="30" height="30" /></span>
        </button>
      </div>
      <p class="video-cap">“AI for Chiropractors in 2026: The Complete Guide + Live Demos” — Winner's Edge Seminar</p>
    </div>
  </section>

  <section class="section gallery-section">
    <div class="container-page">
      <div class="gallery">
        <img src="/img/speaking-a.jpg" width="1400" height="935" alt="Nick Fischer presenting at a chiropractic seminar" loading="lazy" decoding="async" />
        <img src="/img/speaking-b.jpg" width="1400" height="935" alt="Nick Fischer on stage with a microphone" loading="lazy" decoding="async" />
        <img src="/img/speaking-c.jpg" width="1400" height="932" alt="Nick Fischer delivering a keynote talk" loading="lazy" decoding="async" />
      </div>
    </div>
  </section>

  {events.length > 0 && (
    <section class="section section-alt">
      <div class="container-page">
        <div class="section-head" data-reveal>
          <p class="eyebrow">Talks</p>
          <h2 class="h-display">Recent talks &amp; appearances</h2>
        </div>
        <ul class="events">
          {sortedEvents.map((e) => (
            <li class="event" data-reveal>
              <div class="event-main">
                {e.startDate && <time class="event-date" datetime={e.startDate}>{fmtDate(e.startDate)}</time>}
                <span class="event-name">{e.name}</span>
                {(e.event || e.location) && (
                  <span class="event-meta">
                    {e.event && <span class="event-series">{e.event}</span>}
                    {e.event && e.location && <span aria-hidden="true">·</span>}
                    {e.location && <span class="event-loc"><Icon name="lucide:map-pin" width="14" height="14" /> {e.location}</span>}
                  </span>
                )}
              </div>
              {(e.videoUrl || e.url) && (
                <a class="event-link" href={e.videoUrl ?? e.url} target="_blank" rel="noopener">
                  {e.videoUrl ? 'Watch the talk' : 'Event details'} <Icon name="lucide:arrow-up-right" width="16" height="16" />
                </a>
              )}
            </li>
          ))}
        </ul>
      </div>
    </section>
  )}

  <section class="section">
    <div class="container-page faq">
      <h2 class="h-display">Speaking FAQ</h2>
      <dl>
        {faqs.map((f) => (
          <div class="faq-item" data-reveal>
            <dt>{f.question}</dt>
            <dd>{f.answer}</dd>
          </div>
        ))}
      </dl>
    </div>
  </section>

  {/* Click-to-load YouTube facade — the player iframe is only created on click,
      so the page ships zero video JS/iframe until the visitor opts in. */}
  <script is:inline>
    document.querySelectorAll('.video[data-yt]').forEach(function (el) {
      var btn = el.querySelector('.video-btn');
      if (!btn) return;
      btn.addEventListener('click', function () {
        var ifr = document.createElement('iframe');
        ifr.src = 'https://www.youtube-nocookie.com/embed/' + el.getAttribute('data-yt') + '?autoplay=1';
        ifr.title = 'YouTube video player';
        ifr.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';
        ifr.allowFullscreen = true;
        ifr.loading = 'lazy';
        el.innerHTML = '';
        el.appendChild(ifr);
      });
    });
  </script>
</PageLayout>

<style>
  .page-hero { background: linear-gradient(180deg, var(--color-n-50), var(--color-n-0)); padding-block: 3.5rem 3rem; }
  .page-title { font-size: clamp(2rem, 4.5vw, 3.1rem); margin: 0.5rem 0 0; max-width: 20ch; }
  .page-dek { margin-top: 1.25rem; max-width: 52ch; margin-bottom: 1.75rem; }

  .speak-hero-grid { display: grid; grid-template-columns: 1.05fr 0.95fr; gap: 3rem; align-items: center; }
  .speak-hero-media img { width: 100%; aspect-ratio: 4 / 5; object-fit: cover; object-position: 46% center; border-radius: var(--radius-xl); box-shadow: var(--shadow-lg); display: block; }

  .video { position: relative; aspect-ratio: 16 / 9; max-width: 56rem; margin: 0 auto; border-radius: var(--radius-xl); overflow: hidden; box-shadow: var(--shadow-lg); background: #000; }
  .video-btn { position: absolute; inset: 0; width: 100%; height: 100%; padding: 0; border: 0; cursor: pointer; background: none; display: block; }
  .video-btn img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .video-play { position: absolute; inset: 0; margin: auto; width: 76px; height: 76px; display: grid; place-items: center; border-radius: 999px; background: rgba(20,37,59,.85); color: #fff; transition: transform var(--duration-base) var(--ease-out), background var(--duration-base); }
  .video-btn:hover .video-play { transform: scale(1.08); background: var(--color-accent); }
  .video :global(iframe) { width: 100%; height: 100%; border: 0; display: block; }
  .video-cap { text-align: center; color: var(--color-n-500); font-size: 0.9rem; margin: 1rem auto 0; max-width: 56rem; }

  .gallery-section { padding-block: 0 1rem; }
  .gallery { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; }
  .gallery img { width: 100%; height: auto; aspect-ratio: 3 / 2; object-fit: cover; border-radius: var(--radius-lg); box-shadow: var(--shadow-md); display: block; }
  @media (max-width: 860px) { .speak-hero-grid { grid-template-columns: 1fr; } }
  @media (max-width: 680px) { .gallery { grid-template-columns: 1fr; } }

  .section-alt { background: var(--color-n-50); border-block: 1px solid var(--color-n-100); }
  .section-head { max-width: 40ch; margin-bottom: 2.5rem; }
  .section-head h2 { font-size: clamp(1.75rem, 3.5vw, 2.4rem); margin: 0.5rem 0 0; }

  .topics { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.25rem; }
  .topic { padding: 1.75rem; }
  .topic-icon { display: inline-grid; place-items: center; width: 46px; height: 46px; border-radius: var(--radius-md); background: var(--color-gold-100); color: var(--color-gold-600); margin-bottom: 1rem; }
  .topic h3 { font-family: var(--font-display); font-size: 1.2rem; margin: 0 0 0.5rem; }
  .topic p { color: var(--color-n-600); margin: 0; }

  .events { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.85rem; }
  .event {
    display: flex; align-items: center; justify-content: space-between; gap: 1.25rem;
    flex-wrap: wrap; padding: 1.1rem 1.25rem; background: #fff;
    border: 1px solid var(--color-n-200); border-radius: var(--radius-md);
  }
  .event-main { display: flex; flex-direction: column; gap: 0.3rem; }
  .event-date { font-size: 0.8rem; font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; color: var(--color-accent); }
  .event-name { font-family: var(--font-display); font-weight: 700; font-size: 1.1rem; color: var(--color-ink); }
  .event-meta { display: flex; align-items: center; flex-wrap: wrap; gap: 0.4rem; color: var(--color-n-500); font-size: 0.9rem; }
  .event-series { font-weight: 600; color: var(--color-n-600); }
  .event-loc { display: inline-flex; align-items: center; gap: 0.35rem; }
  .event-loc [data-icon] { color: var(--color-n-400); }
  .event-link {
    display: inline-flex; align-items: center; gap: 0.4rem; flex: none;
    font-weight: 600; color: var(--color-accent);
  }
  .event-link:hover { gap: 0.6rem; }

  .faq { max-width: 60rem; }
  .faq h2 { font-size: clamp(1.6rem, 3vw, 2.2rem); margin: 0 0 1.5rem; }
  .faq-item { border-top: 1px solid var(--color-n-200); padding: 1.25rem 0; }
  .faq-item dt { font-family: var(--font-display); font-weight: 700; font-size: 1.1rem; color: var(--color-ink); }
  .faq-item dd { margin: 0.5rem 0 0; color: var(--color-n-600); }

  @media (max-width: 760px) { .topics { grid-template-columns: 1fr; } }
</style>
